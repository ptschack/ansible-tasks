- name: install software
  become: yes
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - "@kde-desktop"
    - kde-standard
    - gimp
    - spectacle
    - vlc
  ignore_errors: yes